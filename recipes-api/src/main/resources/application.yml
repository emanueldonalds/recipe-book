spring:
  profiles:
    active: local
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://authenticate.edonalds.com/auth/realms/recipe-book
          jwk-set-uri: https://authenticate.edonalds.com/auth/realms/recipe-book/protocol/openid-connect/certs
server.port: 8555

